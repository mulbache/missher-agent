# Contributors

Contributor attribution files were removed from this fork. Release notes
resolve authors from git metadata directly (name, or the GitHub login
parsed from `users.noreply.github.com` addresses) without any email →
login mapping table.
